import React from 'react';
import './AlertUser.css'

const AlertUser = (props) => {
    return ( 
        
        <button onClick={props.userAlert}>Click Me</button>
      
        
     );
}
 
export default AlertUser;